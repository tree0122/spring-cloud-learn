server:
  port: 2220
spring:
  application:
    name: compute-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
management:
  security:
    enabled: false