server:
  port: 3335
spring:
  application:
    name: dashboard
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
management:
  security:
    enabled: false
feign:
  hystrix:
    enabled: true